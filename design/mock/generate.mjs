// Responsive design proposal only. No application API calls or mutations.
import { readFileSync, writeFileSync } from 'node:fs'
import { dirname, join } from 'node:path'
import { fileURLToPath } from 'node:url'
const here = dirname(fileURLToPath(import.meta.url))
const credits = JSON.parse(readFileSync(join(here, 'photos/credits.json'), 'utf8'))
const esc = s => String(s).replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('"', '&quot;')
const icon = name => readFileSync(join(here, 'icons', `${name}.svg`), 'utf8').replace('<svg ', '<svg aria-hidden="true" width="22" height="22" ')
const accents = { court: '#3FBA78', cobalt: '#4B72D9', optic: '#D4E24A', clay: '#D9643E' }
const tabs = [['Inicio', 'house'], ['Partido', 'calendar-blank'], ['Ranking', 'chart-bar'], ['Torneos', 'trophy'], ['Perfil', 'user']]
let accent = 'court'
const url = page => `${accent}-${page}.html`
const link = (label, page, cls = 'text-link', glyph = 'arrow-right') => `<a class="${cls}" href="${url(page)}">${label}${icon(glyph)}</a>`
const avatar = (name, cls = '') => `<span class="avatar ${cls}" aria-hidden="true">${name}</span>`
const stat = (value, label) => `<div><strong class="stat-value">${value}</strong><span class="meta">${label}</span></div>`
const photo = (key, content, cls = '') => `<section class="photo ${cls}" style="--focal:${credits[key].focalPoint || 'center'}"><img src="photos/${key}.jpg" alt="" width="1600" height="1067"><div class="photo-content">${content}<a class="photo-credit" href="${url('Creditos')}">Foto de referencia ${icon('info')}</a></div></section>`
const row = (name, detail, end) => `<a class="list-row" href="${url('Partido')}">${avatar(name.split(' ').map(n => n[0]).slice(0, 2).join(''))}<span class="row-copy"><strong>${name}</strong><span class="meta">${detail}</span></span><span class="row-end">${end}</span>${icon('caret-right')}</a>`
const rating = () => `<section class="panel rating"><div><h2 class="meta">Tu nivel de juego</h2><p class="rating-number">2.340 <span>SR</span></p><p class="meta">Oro · Puesto 12 de 184</p>${link('Ver mi ranking', 'Ranking')}</div><img src="ranks/gold.png" width="72" height="72" alt="Nivel Oro"></section>`
const pending = () => photo('clayday', `<span class="status">Resultado pendiente</span><h2>Confirma tu partido<br>con Diego Ramírez</h2><p class="score">6–4, 3–6, 10–7</p><p>La Carolina · Ayer</p>${link('Revisar resultado', 'Resultado', 'button primary', 'check')}`, 'priority')
const upcoming = () => `<section><div class="section-heading"><h2>Tu próximo partido</h2>${link('Ver partido', 'Partido')}</div><div class="list">${row('Andrés Vela', 'Rancho San Francisco, Quito', 'Sáb 19<br>09:00')}</div></section>`
const home = () => `<header class="page-heading"><p class="meta">Quito, viernes 18 de septiembre</p><h1>Buenas tardes, Mateo</h1></header><div class="home-grid"><div class="flow main-column"><div data-pending>${pending()}</div><section data-clear hidden class="panel flow"><span class="status">Todo al día</span><h2>Tu próximo punto empieza aquí</h2><p class="meta">Tienes un partido el sábado. También puedes organizar el siguiente.</p>${link('Programar partido', 'Programar', 'button primary', 'plus')}</section><div class="quick-actions">${link('Programar partido', 'Programar', 'button secondary', 'plus')}${link('Buscar rival', 'Buscar', 'text-link', 'magnifying-glass')}</div>${upcoming()}</div><aside class="flow side-column">${rating()}<section class="stats panel" aria-label="Estadísticas recientes">${stat('4', 'Victorias seguidas')}${stat('+36 SR', 'Este mes')}</section><div class="prototype-controls"><label for="home-state">Escenario del prototipo</label><select id="home-state"><option value="pending">Resultado pendiente</option><option value="clear">Sin pendientes</option></select><p class="meta">Datos de ejemplo. No se envía información.</p></div></aside></div>`
const profile = () => `<div class="profile-grid"><div class="flow">${photo('hero', `<div class="profile-tools">${link('Editar perfil', 'Editar', 'button photo-button', 'pencil-simple')}</div><div class="identity">${avatar('MS', 'large')}<h1>Mateo Salazar</h1><p>@mateosalazar</p><p>Tierra batida los fines de semana.<br>Busco rivales en Quito.</p><p class="location">${icon('map-pin')} Quito, Ecuador</p></div>`, 'profile-hero')}<section class="panel"><div class="stats three">${stat('Oro', 'Nivel actual')}${stat('#12', 'En Ecuador')}${stat('4', 'Victorias seguidas')}</div><details class="more-stats"><summary>Ver estadísticas</summary><div class="stats three">${stat('+36 SR', 'Este mes')}${stat('27', 'Partidos jugados')}${stat('Top 7%', 'En Ecuador')}</div></details></section></div><div class="flow"><section><div class="section-heading"><h2>Último resultado</h2></div>${photo('clayday', `<span class="status">Victoria</span><h2>Contra Lucía Mora</h2><p class="score">+28 SR</p><p>6–4, 6–3</p><p>La Carolina · Hace 3 días</p>${link('Ver partido', 'Historial', 'button photo-button')}`)}</section>${upcoming()}</div></div>`
const players = [ ['Camila Ortiz','Quito','3.120',1], ['Sebastián Paz','Guayaquil','3.045',2], ['Nicolás Herrera','Cuenca','2.980',3], ['Lucía Mora','Manta','2.366',11], ['Mateo Salazar','Quito','2.340',12], ['Diego Ramírez','Quito','2.318',13] ]
const rankRow = ([name, city, sr, rank]) => `<div class="rank-row ${rank === 12 ? 'own-row' : ''}"><span class="rank">${rank}</span>${avatar(name.split(' ').map(n => n[0]).join(''))}<span class="row-copy"><strong>${name}${rank === 12 ? ' (tú)' : ''}</strong><span class="meta">${city}</span></span><span class="rank-score">${sr}<small>SR</small></span></div>`
const ranking = () => `${photo('aerial', '<p>184 jugadores en Ecuador</p><h1>Ranking</h1>', 'compact-hero')}<div class="section-heading"><h2>Clasificación nacional</h2><span class="meta">Temporada actual</span></div><p class="meta">Vista de ejemplo: puestos destacados y jugadores cerca de ti.</p><div class="ranking-grid"><section class="list" aria-label="Primeros tres puestos">${players.slice(0, 3).map(rankRow).join('')}</section><section class="list" aria-label="Cerca de tu posición">${players.slice(3).map(rankRow).join('')}</section></div>`
const match = () => `<header class="page-heading">${link('Volver a inicio', 'Inicio', 'text-link', 'arrow-left')}<h1>Tu partido</h1></header><div class="detail-grid"><div class="flow">${photo('claynight', '<span class="status">Programado</span><h2>Sábado 19 de septiembre</h2><p class="score">09:00</p><p>Club Rancho San Francisco, Quito</p>', 'match-hero')}<section class="panel"><div class="players"><div>${avatar('MS','large')}<h2>Mateo Salazar</h2><p class="meta">Oro · 2.340 SR</p></div><span class="meta">vs</span><div>${avatar('AV','large')}<h2>Andrés Vela</h2><p class="meta">Platino · 2.715 SR</p></div></div><p class="meta">Cancha 3. Al mejor de 3 sets.<br>Partido competitivo que cuenta para el ranking.</p></section></div><aside class="flow"><section class="panel"><h2>Impacto estimado</h2><div class="stats">${stat('+28 SR','Si ganas')}${stat('−19 SR','Si pierdes')}</div><p class="meta">La variación final depende del resultado confirmado.</p></section><section class="panel flow"><h2>Coordina tu partido</h2><button class="button primary" data-demo="El formulario de resultado se diseñará en la siguiente fase.">Registrar resultado ${icon('check')}</button><button class="button secondary" data-demo="La coordinación por WhatsApp se conectará en la aplicación.">WhatsApp ${icon('chat-circle')}</button><button class="text-link" data-demo="El flujo de reprogramación se diseñará en la siguiente fase.">Proponer otra fecha ${icon('clock')}</button></section></aside></div>`
const result = () => `<header class="page-heading">${link('Volver a inicio','Inicio','text-link','arrow-left')}<h1>Revisa el resultado</h1><p class="meta">Tu partido con Diego Ramírez. La Carolina, 17 de septiembre.</p></header><section class="panel flow settings"><h2>Marcador propuesto</h2><p class="score">6–4, 3–6, 10–7</p><p>Victoria propuesta para Mateo Salazar.</p><button class="button primary" data-demo="Vista de ejemplo. No se confirma ni guarda ningún resultado.">Confirmar resultado ${icon('check')}</button><button class="text-link" data-demo="El flujo de corrección se diseñará en la siguiente fase.">Solicitar corrección</button></section>`
const settings = () => `<header class="page-heading"><h1>Apariencia</h1><p class="meta">La misma identidad, con la luz que prefieras.</p></header><section class="panel settings"><fieldset><legend>Tema visual</legend><label class="theme-choice"><input type="radio" name="appearance" value="dark" checked><span class="swatch dark"></span>Graphite</label><label class="theme-choice"><input type="radio" name="appearance" value="light"><span class="swatch light"></span>Claro</label></fieldset><p class="meta">Geist y verde cancha en ambos modos. Tu elección se guarda solo en este navegador para el prototipo.</p></section>`
const creditPage = () => `<header class="page-heading"><h1>Fotografía</h1><p class="meta">Imágenes de referencia. No representan los clubes ni los partidos mencionados en este prototipo.</p></header><div class="credit-grid">${Object.entries(credits).map(([key,c]) => `<article class="panel"><img class="credit-image" src="photos/${key}.jpg" alt="${esc(c.description || c.file)}" loading="lazy"><h2>${esc(c.author)}</h2><p>${esc(c.file.replace('File:',''))}</p><p class="meta">${esc(c.license)}. Presentación: recorte visual y capa oscura, sin modificar el archivo original.</p><a class="text-link" href="${esc(c.page)}">Fuente original ${icon('arrow-right')}</a><a class="text-link" href="https://creativecommons.org/licenses/by-sa/${c.license.endsWith('4.0') ? '4.0' : '3.0'}/">Licencia</a></article>`).join('')}</div>`
const placeholder = (title, detail) => `<header class="page-heading"><h1>${title}</h1><p class="meta">${detail}</p></header><section class="panel flow"><h2>Flujo pendiente de diseño</h2><p>Esta vista muestra el destino de la navegación. No contiene funciones conectadas.</p>${link('Volver a inicio','Inicio','button secondary','arrow-left')}</section>`
const render = (page, content) => `<!doctype html><html lang="es" data-appearance="dark"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><title>${page} · Tenis Ecuador</title><link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700&family=Geist+Mono:wght@500&display=swap" rel="stylesheet"><link rel="stylesheet" href="mock.css"><script src="mock.js" defer></script></head><body style="${accent !== 'court' ? `--accent:${accents[accent]};--accent-foreground:${accent === 'cobalt' ? '#fff' : '#07150E'};` : ''}"><a class="skip" href="#main">Saltar al contenido</a><header class="nav-island"><a class="brand" href="${url('Inicio')}">${icon('tennis-ball')}<span>Tenis <strong>Ecuador</strong></span></a><nav aria-label="Principal">${tabs.map(([p]) => `<a href="${url(p)}" ${p === page || page === 'Escritorio' && p === 'Inicio' ? 'aria-current="page"' : ''}>${p === 'Partido' ? 'Partidos' : p}</a>`).join('')}</nav><a class="icon-button" href="${url('Ajustes')}" aria-label="Apariencia">${icon('sliders-horizontal')}</a></header><main id="main" tabindex="-1" class="page">${content}<footer class="footer"><span>Prototipo · Datos de ejemplo</span><a href="${url('Creditos')}">Créditos de fotografías</a><a href="index.html">Índice de diseño</a></footer></main><nav class="tab-island" aria-label="Principal móvil">${tabs.map(([p,i]) => `<a href="${url(p)}" ${p === page || page === 'Escritorio' && p === 'Inicio' ? 'aria-current="page"' : ''}>${icon(i)}<span>${p === 'Partido' ? 'Partidos' : p}</span></a>`).join('')}</nav><dialog id="demo-dialog" aria-labelledby="demo-title" aria-describedby="demo-message"><h2 id="demo-title">Vista de prototipo</h2><p id="demo-message"></p><form method="dialog"><button class="button primary">Entendido</button></form></dialog></body></html>`
for (accent of Object.keys(accents)) {
  const pages = { Inicio: home, Escritorio: home, Perfil: profile, Ranking: ranking, Partido: match, Resultado: result, Ajustes: settings, Creditos: creditPage, Torneos: () => placeholder('Torneos','Encuentra tu próxima competición.'), Programar: () => placeholder('Programar partido','Elige rival, fecha y cancha.'), Buscar: () => placeholder('Buscar rival','Encuentra jugadores de tu nivel en tu ciudad.'), Editar: () => placeholder('Editar perfil','Tu identidad en la comunidad.'), Historial: () => placeholder('Victoria contra Lucía Mora','6–4, 6–3. La Carolina, 15 de septiembre. +28 SR.') }
  for (const [name, build] of Object.entries(pages)) writeFileSync(join(here, `${accent}-${name}.html`), render(name,build()))
}
writeFileSync(join(here, 'index.html'), `<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Tenis Ecuador · Diseño</title><link rel="stylesheet" href="mock.css"></head><body><main class="page"><header class="page-heading"><p class="meta">Revamp · Septiembre de 2026</p><h1>Una cancha.<br>Tu próximo partido.</h1><p>Graphite, verde cancha y fotografía con intención. Todos los diseños se adaptan al ancho de la ventana.</p></header><section class="panel flow"><h2>Dirección elegida</h2><div class="quick-actions">${['Inicio','Perfil','Ranking','Partido','Ajustes','Creditos'].map(p => `<a class="button secondary" href="court-${p}.html">${p}</a>`).join('')}</div><p class="meta">Inicio incluye dos escenarios. Apariencia permite comparar Graphite y su versión clara. Torneos, programación y búsqueda solo muestran destinos pendientes de diseño.</p></section><details class="panel archive"><summary>Exploraciones internas de acento</summary><p>Comparaciones de diseño, no opciones de marca para el producto.</p>${['cobalt','optic','clay'].map(k => `<a class="text-link" href="${k}-Inicio.html">${k}</a>`).join('')}</details></main></body></html>`)
console.log('Generated 52 responsive screens and design index.')
